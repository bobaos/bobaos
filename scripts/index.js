import * as socket from './socketio/index';

/**
 * export custom scripts.
 * app.js imports all of those and runs "start(app)" function
 * export {script1, script2, ..., scriptN};
 */
export {socket};